# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Use npm (package-lock.json) as the single source of truth — bun lockfile breaks
# build platforms that auto-detect it but don't have bun installed.
bun.lockb

# Environment variables (never commit secrets)
.env
.env.local
.env.*.local
supabase/functions/.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.tmp-analysis/

# Claude Code agent memory (local)
/memory/

# Supabase CLI local state (muda sozinho a cada comando — não versionar)
supabase/.temp/
